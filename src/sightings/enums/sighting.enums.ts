export enum SightingType {
  Sighting = 'sighting',
  ConflictZone = 'conflict_zone',
  Habitat = 'habitat',
}

export enum SightingStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
}